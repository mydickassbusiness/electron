name: C/C++ CI

on:
  push:
    branches: [ "mydickassbusiness-patch-5" ]
  pull_request:
    branches: [ "mydickassbusiness-patch-5" ]

jobs:
  build: my drive mode

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: configure
      run: ./configure
    - name: make
      run: make
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
